; Allow better-sqlite3 and esbuild to run their post-install build scripts
; so that `pnpm install` completes without interactive approval prompts.
onlyBuiltDependencies[]=better-sqlite3
onlyBuiltDependencies[]=esbuild
